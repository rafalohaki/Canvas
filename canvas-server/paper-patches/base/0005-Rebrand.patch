From 4ae490e7573df842a04b2ddb59585f04dbbaf84f Mon Sep 17 00:00:00 2001
From: rafalohaki <rafalohaki@gmail.com>
Date: Thu, 25 Jun 2026 17:08:18 +0200
Subject: [PATCH 5/9] Rebrand

---
 .../java/com/destroystokyo/paper/Metrics.java | 12 ++++++++---
 .../paper/PaperServerInternalAPIBridge.java   |  2 +-
 .../io/papermc/paper/ServerBuildInfoImpl.java | 21 ++++++++++++++-----
 .../paper/command/PaperVersionCommand.java    |  8 +++++--
 4 files changed, 32 insertions(+), 11 deletions(-)

diff --git a/src/main/java/com/destroystokyo/paper/Metrics.java b/src/main/java/com/destroystokyo/paper/Metrics.java
index 9f4d73fd3..037e162d7 100644
--- a/src/main/java/com/destroystokyo/paper/Metrics.java
+++ b/src/main/java/com/destroystokyo/paper/Metrics.java
@@ -593,7 +593,7 @@ public class Metrics {
             boolean logFailedRequests = config.getBoolean("logFailedRequests", false);
             // Only start Metrics, if it's enabled in the config
             if (config.getBoolean("enabled", true)) {
-                Metrics metrics = new Metrics("Paper", serverUUID, logFailedRequests, Bukkit.getLogger());
+                Metrics metrics = new Metrics("Canvas", serverUUID, logFailedRequests, Bukkit.getLogger()); // Canvas - Rebrand
 
                 metrics.addCustomChart(new Metrics.SimplePie("minecraft_version", () -> {
                     String minecraftVersion = Bukkit.getVersion();
@@ -607,11 +607,17 @@ public class Metrics {
                 final String implVersion = org.bukkit.craftbukkit.Main.class.getPackage().getImplementationVersion();
                 if (implVersion != null) {
                     final String buildOrHash = implVersion.substring(implVersion.lastIndexOf('-') + 1);
-                    paperVersion = "git-Paper-%s-%s".formatted(Bukkit.getServer().getMinecraftVersion(), buildOrHash);
+                    // Canvas start - new versioning schem for bstats
+                    io.papermc.paper.ServerBuildInfo buildInfo = io.papermc.paper.ServerBuildInfo.buildInfo();
+                    final int num = buildInfo.buildNumber().getAsInt();
+                    paperVersion = "Canvas-V3-" +
+                        buildInfo.minecraftVersionName() + "-" +
+                        (num != -1 ? num : "DEV");
+                    // Canvas end - new versioning schem for bstats
                 } else {
                     paperVersion = "unknown";
                 }
-                metrics.addCustomChart(new Metrics.SimplePie("paper_version", () -> paperVersion));
+                metrics.addCustomChart(new Metrics.SimplePie("canvas_version", () -> paperVersion)); // Canvas - Rebrand
 
                 metrics.addCustomChart(new Metrics.DrilldownPie("java_version", () -> {
                     Map<String, Map<String, Integer>> map = new HashMap<>();
diff --git a/src/main/java/io/papermc/paper/PaperServerInternalAPIBridge.java b/src/main/java/io/papermc/paper/PaperServerInternalAPIBridge.java
index f4d5123d9..3fcdea18a 100644
--- a/src/main/java/io/papermc/paper/PaperServerInternalAPIBridge.java
+++ b/src/main/java/io/papermc/paper/PaperServerInternalAPIBridge.java
@@ -181,7 +181,7 @@ public class PaperServerInternalAPIBridge implements InternalAPIBridge {
 
     @Override
     public VersionFetcher getVersionFetcher() {
-        return new PaperVersionFetcher();
+        return new io.canvasmc.canvas.util.version.CanvasVersionFetcher(); // Canvas - Rebrand
     }
 
     @Override
diff --git a/src/main/java/io/papermc/paper/ServerBuildInfoImpl.java b/src/main/java/io/papermc/paper/ServerBuildInfoImpl.java
index 74ffdc823..9cd0677f0 100644
--- a/src/main/java/io/papermc/paper/ServerBuildInfoImpl.java
+++ b/src/main/java/io/papermc/paper/ServerBuildInfoImpl.java
@@ -21,7 +21,11 @@ public record ServerBuildInfoImpl(
     OptionalInt buildNumber,
     Instant buildTime,
     Optional<String> gitBranch,
-    Optional<String> gitCommit
+    // Canvas start - improve version command
+    Optional<String> gitCommit,
+    Optional<String> brandVendor,
+    Optional<String> brandWebsite
+    // Canvas end - improve version command
 ) implements ServerBuildInfo {
     private static final String ATTRIBUTE_BRAND_ID = "Brand-Id";
     private static final String ATTRIBUTE_BRAND_NAME = "Brand-Name";
@@ -29,8 +33,11 @@ public record ServerBuildInfoImpl(
     private static final String ATTRIBUTE_BUILD_NUMBER = "Build-Number";
     private static final String ATTRIBUTE_GIT_BRANCH = "Git-Branch";
     private static final String ATTRIBUTE_GIT_COMMIT = "Git-Commit";
+    private static final String ATTRIBUTE_BRAND_VENDOR = "Brand-Vendor"; // Canvas - improve version command
+    private static final String ATTRIBUTE_BRAND_WEBSITE = "Brand-Website"; // Canvas - improve version command
 
     private static final String BRAND_PAPER_NAME = "Paper";
+    private static final String BRAND_CANVAS_NAME = "Canvas"; // Canvas - Rebrand
 
     private static final String BUILD_DEV = "DEV";
 
@@ -42,9 +49,9 @@ public record ServerBuildInfoImpl(
         this(
             getManifestAttribute(manifest, ATTRIBUTE_BRAND_ID)
                 .map(Key::key)
-                .orElse(BRAND_PAPER_ID),
+                .orElse(BRAND_CANVAS_ID), // Canvas - Rebrand
             getManifestAttribute(manifest, ATTRIBUTE_BRAND_NAME)
-                .orElse(BRAND_PAPER_NAME),
+                .orElse(BRAND_CANVAS_NAME), // Canvas - Rebrand
             SharedConstants.getCurrentVersion().id(),
             SharedConstants.getCurrentVersion().name(),
             getManifestAttribute(manifest, ATTRIBUTE_BUILD_NUMBER)
@@ -55,13 +62,17 @@ public record ServerBuildInfoImpl(
                 .map(Instant::parse)
                 .orElse(Main.BOOT_TIME),
             getManifestAttribute(manifest, ATTRIBUTE_GIT_BRANCH),
-            getManifestAttribute(manifest, ATTRIBUTE_GIT_COMMIT)
+            // Canvas start - improve version command
+            getManifestAttribute(manifest, ATTRIBUTE_GIT_COMMIT),
+            getManifestAttribute(manifest, ATTRIBUTE_BRAND_VENDOR),
+            getManifestAttribute(manifest, ATTRIBUTE_BRAND_WEBSITE)
+            // Canvas end - improve version command
         );
     }
 
     @Override
     public boolean isBrandCompatible(final @NotNull Key brandId) {
-        return brandId.equals(this.brandId);
+        return brandId.equals(this.brandId) || brandId.equals(BRAND_PAPER_ID) || brandId.equals(BRAND_FOLIA_ID); // Canvas - Rebrand
     }
 
     @Override
diff --git a/src/main/java/io/papermc/paper/command/PaperVersionCommand.java b/src/main/java/io/papermc/paper/command/PaperVersionCommand.java
index 5f17c295e..1df07aa47 100644
--- a/src/main/java/io/papermc/paper/command/PaperVersionCommand.java
+++ b/src/main/java/io/papermc/paper/command/PaperVersionCommand.java
@@ -170,9 +170,13 @@ public class PaperVersionCommand {
                this.versionFetcher.getVersionMessage()
            );
 
+           // Canvas start - improve version command
+           final Component bukkitComponent = message.hoverEvent(Component.translatable("chat.copy.click", NamedTextColor.WHITE))
+               .clickEvent(ClickEvent.copyToClipboard(PlainTextComponentSerializer.plainText().serialize(message)));
+           final Component versionFetcherComponent = this.versionFetcher.getFullOutMessage();
            return new ComputedVersion(
-               message.hoverEvent(Component.translatable("chat.copy.click", NamedTextColor.WHITE))
-                   .clickEvent(ClickEvent.copyToClipboard(PlainTextComponentSerializer.plainText().serialize(message))),
+               versionFetcherComponent == null ? bukkitComponent : versionFetcherComponent,
+           // Canvas end - improve version command
                System.currentTimeMillis()
            );
        });
-- 
2.54.0 (Apple Git-157)

